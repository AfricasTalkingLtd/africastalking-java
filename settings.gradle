/**
 * Africa's Talking SDK
 */

/* Libs */
Set<String> libs = new HashSet<String>()
libs << 'sms'
libs << 'payments'
libs << 'token'
libs << 'airtime'
libs << 'voice'
libs << 'ussd'
libs << 'account'
libs << 'common'
libs << 'core'

libs.each { lib ->
    include lib
    project(":${lib}").projectDir = file("libs/${lib}")
}

/* Examples */
include 'example'

